package com.albasil.todolist.DB

import androidx.lifecycle.LiveData
import com.albasil.todolist.TaskDao

class TaskRepository (private val taskDao: TaskDao){
   // val readAllData: LiveData<List<DataTask>> = tas
}